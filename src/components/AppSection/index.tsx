import styles from "./app_section.module.scss";

const AppSection = () => (
  <section className={styles.section}>
    <div className={styles.wrapper}>
      <h2 className={styles.title}>Thousands of 5 Star Reviews...</h2>
    </div>
  </section>
);

export { AppSection };
